import os
import json
import selected_post.data as SelectedPostData
import selected_user.data as SelectedUserData
import loggedin_user.data as LoggedUserData

def export_logged_user_data_text(basic_data = True, following_list = True, followers_list = True, posts_list = True):
    if basic_data == True:
        #something
        pass

    if following_list == True:
        #something
        pass

    if followers_list == True:
        #something
        pass

    if posts_list == True:
        #something
        pass

    if (basic_data == False) and (following_list == False) and (followers_list == False) and (posts_list == False):
        #something
        pass

def export_selected_user_data_text(basicd_data = True, following_list = True, followers_list = True, posts_list = True):
    pass

def export_selected_post_data_text(basic_data = True, likers_list = True):
    pass